# Get a key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Lets the eval suite bypass rate limits so a re-run doesn't throttle itself.
# Leave unset in production and the bypass is inert.
RATE_LIMIT_BYPASS_TOKEN=any-long-random-string
